# iRightMouse Pro 集成

将「返回上一级」加入 iRightMouse Pro 工具箱：

1. 运行 `bash scripts/install.sh` 完成安装
2. 打开 **iRightMouse Pro → 偏好设置 → 工具箱**
3. 添加 **打开 App** 类型
4. 名称填写 **返回上一级**
5. 选择应用 `~/Applications/返回上一级.app`
6. 启用该项

## 说明

- 首次使用若系统提示自动化权限，请允许控制 **Finder**
- 已在根目录时不会跳转；无访达窗口时不执行
- 访达自带 **⌘↑** 同样可用，无需安装
